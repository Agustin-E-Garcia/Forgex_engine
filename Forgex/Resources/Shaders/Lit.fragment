#version 330 core
in vec3 fragNormal;
in vec3 fragAlbedo;
out vec4 FragColor;

void main()
{
    vec3 lightDir = normalize(vec3(1.0, 3.0, 1.0));
    float ambient = 0.2;
    float diffuse = max(dot(fragNormal, lightDir), 0.0);

    FragColor = vec4(fragAlbedo * (ambient + diffuse), 1.0);
}
